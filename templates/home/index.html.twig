{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}

{% block body %}
<div class="col-md-8">
    <h3>Last Articles</h3>
    <hr>
    {% for post in posts %}
        <a href="{{ path("post_show", {'id': post.id}) }}" class="custom-card">
            <div class="card mb-2">
                <div class="card-body">
                    {{ post.content }}
                    <div class="text-muted">
                        ─ {{ post.author }} | {{ post.createdAt|date('Y-m-d') }}
                    </div>
                </div>
            </div>
        </a>
    {% endfor %}
</div>
{% endblock %}

{% block sidebar %}
    <div class="col-md-4 bg-light p-3">
        <div class="row mb-3">
            <div>Charlotte</div>
        </div>
        <div class="row">
            <h3>Top Articles</h3>
            <hr>
            {% for post in posts %}
                <a href="{{ path("post_show", {'id': post.id}) }}" class="custom-card mb-2">
                    <div class="card">
                        <div class="card-body">
                            {{ post.content }}
                            <div class="text-muted">
                                ─ {{ post.author }} | {{ post.createdAt|date('Y-m-d') }}
                            </div>
                        </div>
                    </div>
                </a>
            {% endfor %}
        </div>
    </div>
{% endblock %}
